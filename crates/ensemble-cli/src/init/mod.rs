use std::process::ExitCode;

/// Run the interactive `ensemble init` wizard.
///
/// This is a placeholder implementation. The full wizard will guide the user
/// through creating an `ensemble.yaml` configuration file interactively.
pub async fn run_wizard() -> ExitCode {
    ExitCode::SUCCESS
}
