[package]
name = "ensemble-ui"
version = "0.0.1"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

# React dashboard SPA, built with pnpm/Vite. Source lives in src-ui/.
