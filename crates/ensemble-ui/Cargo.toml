[package]
name = "ensemble-ui"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

# React dashboard SPA, built with npm/Vite. Source lives in src-ui/.
