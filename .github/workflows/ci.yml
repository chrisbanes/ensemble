name: CI

on:
  push:
    branches: [main]
    tags: ["v[0-9]*.[0-9]*.[0-9]*"]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * *"

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  ci:
    name: Check, Test, Clippy, Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2

      # Cheapest checks first — fail fast
      - name: Verify Cargo package licenses
        run: |
          for package in ensemble-core ensemble-cli ensemble-desktop; do
            cmp LICENSE "crates/$package/LICENSE"
            cargo package -p "$package" --list | grep -Fxq LICENSE
          done

      - name: Format
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --exclude ensemble-desktop -- -D warnings

      - name: Test
        run: cargo test --workspace --exclude ensemble-desktop

      - name: Product E2E
        run: cargo test -p ensemble-cli --features web-ui --test product_e2e -- --nocapture
        env:
          SKIP_UI_BUILD: 1

      - name: Check CLI web UI feature
        run: cargo check -p ensemble-cli --features web-ui
        env:
          SKIP_UI_BUILD: 1

  msrv:
    name: MSRV (Rust 1.95.0)
    runs-on: ubuntu-latest
    env:
      RUSTUP_TOOLCHAIN: 1.95.0
      RUSTFLAGS: ""
      SKIP_UI_BUILD: 1
    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@1.95.0

      - uses: Swatinem/rust-cache@v2
        with:
          key: msrv-1.95.0

      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Check minimum supported Rust version
        run: cargo +1.95.0 check --workspace --all-targets

  frontend:
    name: Frontend Test and Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          package_json_file: crates/ensemble-ui/src-ui/package.json

      - uses: actions/setup-node@v7
        with:
          node-version: 24.20.0
          cache: pnpm
          cache-dependency-path: crates/ensemble-ui/src-ui/pnpm-lock.yaml

      - uses: Swatinem/rust-cache@v2

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile
        working-directory: crates/ensemble-ui/src-ui

      - name: Generate frontend API client
        run: pnpm run codegen
        working-directory: crates/ensemble-ui/src-ui

      - name: Frontend unit tests
        run: pnpm test
        working-directory: crates/ensemble-ui/src-ui

      - name: Frontend build
        run: pnpm run build:embed
        working-directory: crates/ensemble-ui/src-ui

      - name: Upload frontend dist
        uses: actions/upload-artifact@v7
        with:
          name: frontend-dist
          path: crates/ensemble-ui/src-ui/dist/
          retention-days: 1

  # Fast PR/desktop compile job. Runs in parallel with `ci` and `frontend`
  # (no `needs:`), so PR feedback is bounded by the slowest of the three.
  # Decision from Task 1: cargo test -p ensemble-desktop links the bin target
  # which uses tauri::Builder with WebviewWindowBuilder, so on Linux webkit2gtk
  # must be installed for the test binary to link and run.
  tauri-pr:
    name: Tauri PR (${{ matrix.name }})
    if: "!startsWith(github.ref, 'refs/tags/v')"
    strategy:
      matrix:
        include:
          - name: Linux
            runner: ubuntu-latest
          - name: Mac
            runner: macos-latest
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2

      - name: Install Tauri system dependencies (Linux)
        if: matrix.runner == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xvfb

      - name: Run desktop check
        run: cargo check -p ensemble-desktop --all-targets
        env:
          SKIP_UI_BUILD: 1

      - name: Run desktop unit tests
        run: cargo test -p ensemble-desktop --bin ensemble-desktop -- --nocapture
        env:
          SKIP_UI_BUILD: 1

      - name: Run E2E smoke test (verify app launches)
        run: ${{ matrix.runner == 'ubuntu-latest' && 'xvfb-run -a' || '' }} cargo test -p ensemble-desktop --test e2e app_launches_without_crash -- --ignored --nocapture
        env:
          SKIP_UI_BUILD: 1
        timeout-minutes: 2

  # Full Tauri bundle (dmg/deb/AppImage). Runs on:
  #   - push to main (any branch push; no paths filter)
  #   - schedule (daily 06:00 UTC) to catch packaging regressions
  #   - push of a tag
  # PRs do NOT hit this job; PR feedback is the tauri-pr job above.
  tauri-bundle:
    name: Tauri Bundle (${{ matrix.name }})
    needs: frontend
    if: |
      github.event_name == 'schedule' ||
      (github.event_name == 'push' && (github.ref_type == 'tag' || github.ref_type == 'branch'))
    strategy:
      matrix:
        include:
          - name: Linux
            runner: ubuntu-latest
          - name: Mac
            runner: macos-latest
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v7

      - uses: Swatinem/rust-cache@v2

      - name: Download frontend dist
        uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: crates/ensemble-ui/src-ui/dist/

      - name: Copy dist to desktop assets for rust-embed
        run: |
          mkdir -p crates/ensemble-desktop/assets/spa
          cp -r crates/ensemble-ui/src-ui/dist/* crates/ensemble-desktop/assets/spa/

      # pnpm + node needed for tauri CLI install
      - uses: pnpm/action-setup@v6
        with:
          package_json_file: crates/ensemble-ui/src-ui/package.json

      - uses: actions/setup-node@v7
        with:
          node-version: 24.20.0

      - name: Install Tauri CLI
        run: pnpm install -g @tauri-apps/cli

      - name: Install Tauri system dependencies (Linux)
        if: matrix.runner == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xvfb

      - name: Run desktop unit tests
        run: cargo test -p ensemble-desktop --bin ensemble-desktop -- --nocapture
        env:
          SKIP_UI_BUILD: 1

      - name: Build Tauri app
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        run: tauri build
        working-directory: crates/ensemble-desktop

      - name: Build Tauri app (signed)
        if: startsWith(github.ref, 'refs/tags/v')
        run: tauri build
        working-directory: crates/ensemble-desktop
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}

      - name: Run E2E smoke test (verify app launches)
        run: ${{ matrix.runner == 'ubuntu-latest' && 'xvfb-run -a' || '' }} cargo test -p ensemble-desktop --test e2e app_launches_without_crash -- --ignored --nocapture
        env:
          SKIP_UI_BUILD: 1
        timeout-minutes: 2

      - name: Upload desktop artifacts
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@v7
        with:
          name: desktop-${{ matrix.name }}
          path: |
            target/**/*.dmg
            target/**/*.deb
            target/**/*.AppImage
          retention-days: 7

  # --- Release-only jobs (tag pushes) ---

  build-cli:
    name: CLI (${{ matrix.target }})
    if: startsWith(github.ref, 'refs/tags/v')
    needs: frontend
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            runner: macos-latest
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust target
        run: rustup target add "${{ matrix.target }}"

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Download frontend dist
        uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: crates/ensemble-cli/assets/spa/

      - name: Build CLI
        run: cargo build --release -p ensemble-cli --features web-ui --target ${{ matrix.target }}

      - name: Package binary
        run: |
          ARCHIVE="ensemble-${{ github.ref_name }}-${{ matrix.target }}.tar.gz"
          PACKAGE_DIR="target/${{ matrix.target }}/package"
          mkdir -p "$PACKAGE_DIR"
          cp "target/${{ matrix.target }}/release/ensemble" "$PACKAGE_DIR/ensemble"
          cp LICENSE "$PACKAGE_DIR/LICENSE"
          tar -czf "$ARCHIVE" -C "$PACKAGE_DIR" ensemble LICENSE
          tar -tzf "$ARCHIVE" | grep -Fxq LICENSE
          echo "archive=$ARCHIVE" >> "$GITHUB_ENV"

      - uses: actions/upload-artifact@v7
        with:
          name: cli-${{ matrix.target }}
          path: ${{ env.archive }}

  release:
    name: Release & Homebrew
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-cli, tauri-bundle]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          pattern: cli-*
          merge-multiple: true

      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          pattern: desktop-*
          merge-multiple: true

      - uses: softprops/action-gh-release@v3
        with:
          files: |
            artifacts/**
            LICENSE
          generate_release_notes: true

      - name: Update Homebrew tap
        env:
          TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          TAG: ${{ github.ref_name }}
          REPO: ${{ github.repository }}
          ARTIFACTS_DIR: artifacts
        run: .github/update-homebrew.sh
