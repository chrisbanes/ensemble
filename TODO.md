# TODO

- [ ] Post rejection summaries to issue tracker via `add_comment` when an agent sends a `Verdict::Reject` with a summary
- [ ] Wire orchestrator poll loop into CLI (`ensemble run`) — code exists in `orchestrator/mod.rs`, not yet spawned
- [ ] Integrate Tauri desktop app with ensemble-core (currently a bare scaffold)
- [ ] Homebrew formula for distribution
